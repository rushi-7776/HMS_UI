import { Component } from '@angular/core';

@Component({
  selector: 'app-appointment',
  standalone: true, // This should be true for standalone components
  templateUrl: './appointment.component.html', // Adjust the path if needed
  styleUrls: ['./appointment.component.css']
})
export class AppointmentComponent {}
